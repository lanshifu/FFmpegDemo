package io.reactivex;

import defpackage.wk;
import io.reactivex.disposables.b;

/* compiled from: ObservableEmitter */
public interface m<T> extends d<T> {
    boolean isDisposed();

    void setCancellable(wk wkVar);

    void setDisposable(b bVar);
}
